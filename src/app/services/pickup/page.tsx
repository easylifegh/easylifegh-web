"use client"

import PageTemplate from "@/components/PageTemplate"

export default function PickupPage() {
  return (
    <PageTemplate
      titleKey="pages.pickup.title"
      subtitleKey="pages.pickup.subtitle"
    />
  )
}
