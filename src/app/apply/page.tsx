"use client"

import PageTemplate from "@/components/PageTemplate"

export default function ApplyPage() {
  return (
    <PageTemplate
      titleKey="pages.apply.title"
      subtitleKey="pages.apply.subtitle"
    />
  )
}
